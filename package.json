{
  "name": "cards-tests",
  "private": true,
  "description": "jsdom harnesses for the docs/ front-end. Not shipped — GitHub Pages serves docs/ as plain static files.",
  "scripts": {
    "test": "./tests/run-all.sh"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
